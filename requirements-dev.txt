# Install the package with all dev extras:
#   pip install -e ".[dev]"
#
# Listed explicitly here for tools (Dependabot, pip-audit) that read requirements files.
pytest>=8.0
pytest-cov>=5.0
build>=1.0
